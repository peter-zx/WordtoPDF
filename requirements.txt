# WordtoPDF 项目依赖
# 核心依赖
streamlit>=1.28.0
pandas>=2.0.0
openpyxl>=3.1.0
python-docx>=0.8.11

# 文档处理
beautifulsoup4>=4.12.0
striprtf>=0.0.26

# Windows专用（本地开发）
pywin32>=306; sys_platform == 'win32'
comtypes>=1.2.0; sys_platform == 'win32'
docx2pdf>=0.1.8; sys_platform == 'win32'

# 跨平台文档转换
pypandoc>=1.11; sys_platform != 'win32'

# PDF处理（可选）
# reportlab>=4.0.0
# PyPDF2>=3.0.0
